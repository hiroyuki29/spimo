import 'package:flutter/material.dart';

class MemosHomeScreen extends StatelessWidget {
  const MemosHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Text('memos'),
    );
  }
}
